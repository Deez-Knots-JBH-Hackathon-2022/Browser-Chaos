{
  "name": "gpt-3-relay",
  "version": "0.1.0",
  "description": "Receives HTTP requests from the browser extension and returns information from GPT-3 API",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.0",
    "openai": "^2.0.5"
  }
}
